# Cloudflare Pages / Wrangler local dev — secrets file. NEVER commit this.
.dev.vars
.dev.vars.*

# Wrangler local state
.wrangler/
.mf/

# Node / editor noise (in case tooling shows up later)
node_modules/
.DS_Store
*.log

# Local working documents (review notes, hand-off prompts, finished specs).
# Swept into commits twice by `git add -A`; keep them local.
CODE_REVIEW.md
HANDOFF_PROMPT.md
SPEC_wind_graph.md

# Golden-master harness: dev-only, installed per checkout
tools/golden-harness/node_modules/
